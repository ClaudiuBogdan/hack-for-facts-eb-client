import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/achizitii/metodologie')({
  staticData: {
    title: 'Metodologia valorilor din achiziții publice',
  },
})
