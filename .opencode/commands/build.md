---
description: Build the app bundle without docs
agent: qa
subtask: true
---

Run `yarn build:app`.

If the build fails, separate TypeScript, Vite, and Lingui failures and identify the minimal follow-up work.
